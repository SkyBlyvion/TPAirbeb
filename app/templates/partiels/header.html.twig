<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
   <style>
      /* Style des pipes */
      .pipe::before {
         content: "|";
         margin: 0 8px; /* Ajustez la marge selon vos préférences */
         color: #ffffff; /* Couleur des pipes */
      }
   </style>
   <a class="navbar-brand" href="{{ path('accueil') }}">Airbeb</a>
   <button class="navbar-toggler" type="button" data-toggle="collapse" 
   data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
   <span class="navbar-toggler-icon"></span>
   </button>
   <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav mr-auto">
         <li class="nav-item">
            <a class="nav-link" href="{{ path('accueil') }}">Accueil</a>
         </li>
         <li class="nav-item">
            <a class="nav-link" href="{{ path('annonces') }}">Annonces</a>
         </li>
      </ul>
      <ul class="navbar-nav ml-auto">
         {% if app.user %}
         <li class="">
            
            {# TODO: ajouter le form insription et login / si connecté dropdown sinon inscription #}
            <div>
               <a href="{{ path('accueil') }}" class="">Créer une annonce</a>
               <span class="pipe"></span>
               <a href="{{ path('accueil') }}" class="">Mon compte</a>
               <span class="pipe"></span>
               <a href="{{ path('accueil') }}" class="">Modifier mon profil</a>
               <span class="pipe"></span>
               <a href="{{ path('app_logout') }}" class="">Déconnexion</a>
            </div>
         </li>
         {% else %}
         <li class="nav-item">
            <a href="{{ path('app_register') }}" class="nav-link">Inscription</a>
         </li>
         <li class="nav-item">
            <a href="{{ path('app_login') }}" class="nav-link">Connexion</a>
         </li>
         {% endif %}
      </ul>
   </div>
</nav>
{% for label, messages in app.flashes %}
<div class="container">
   <div class="alert alert-{{ label }}">
      {% for message in messages %}
      <p>{{ message | raw }}</p>
      {% endfor %}
   </div>
</div>
{% endfor %}